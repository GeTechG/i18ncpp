# v0.4.2 Correctness & Portability — Archive

**Archived:** 2026-04-13
**Status:** Complete

---

## Focus

Close all findings from REVIEW-2026-04-11 (5 bugs + 4 coverage gaps) and make the library buildable on GCC/Clang toolchains. No new features. No CI.

## Phases

| Phase | Name | Plans | Status | Completed |
|-------|------|-------|--------|-----------|
| 1 | Correctness | 1 (01-01) | ✅ Complete | 2026-04-12 |
| 2 | Portability | 1 (02-01) | ✅ Complete | 2026-04-13 |

### Phase 1: Correctness

Closed: F1 (load replace), F2 (baselineConfig_ format isolation), F3 (bool branch order in toString), F4 (keyExists parity) + regression tests G1–G4.

### Phase 2: Portability

Closed: F5 — portable localtime shim (`localtime_s`/`localtime_r`), `std::format`/`{fmt}` fallback via `i18n_fmt` alias + CMake FetchContent probe, toolchain matrix documented.

## Deferred to v0.5

- `load(const json&)` partial-merge localesData rollback (cache invariant preserved; merged locales from earlier iterations remain)
- Windows Unicode-path regression test with `日本語.json` fixture
- Actual GCC/Clang CI validation (source-level compile-compat only in v0.4.2)
