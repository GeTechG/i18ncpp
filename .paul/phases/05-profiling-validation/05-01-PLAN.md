---
phase: 05-profiling-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [benchmarks/BASELINE.md]
autonomous: true
---

<objective>
## Goal
Re-run all 18 micro-benchmarks against the optimized codebase (Phases 2-4), compare results with the Phase 1 baseline, and produce a final performance report documenting improvements and any remaining hot spots.

## Purpose
Validates that the v0.3 optimization work (move semantics, buffer pooling, fold expressions, translation result cache) delivered measurable performance gains. Closes the optimization loop with data.

## Output
- Updated `benchmarks/BASELINE.md` with before/after comparison table
- Console benchmark output confirming current timings
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/01-benchmarks/01-01-SUMMARY.md — baseline data and benchmark harness design

## Source Files
@benchmarks/bench_harness.h
@benchmarks/bench_lookup.cpp
@benchmarks/bench_interpolation.cpp
@benchmarks/bench_formatting.cpp
@benchmarks/BASELINE.md
</context>

<acceptance_criteria>

## AC-1: Benchmarks build and run successfully
```gherkin
Given the optimized codebase with Phases 2-4 changes
When benchmarks are built with cmake -DBUILD_BENCHMARKS=ON in Release mode
Then all 3 benchmark executables (bench_lookup, bench_interpolation, bench_formatting) compile and run without errors
```

## AC-2: Before/after comparison documented
```gherkin
Given the Phase 1 baseline data in BASELINE.md
When current benchmark results are collected
Then BASELINE.md contains a comparison table with baseline ns/op, current ns/op, and percentage change for all 18 benchmarks
```

## AC-3: Key optimization targets show improvement
```gherkin
Given named interpolation baseline was 6775 ns/op and positional was 1545 ns/op
When current benchmarks are measured
Then the comparison report accurately reflects the measured changes and identifies any remaining hot spots
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Build and run all benchmarks in Release mode</name>
  <files>benchmarks/bench_lookup.cpp, benchmarks/bench_interpolation.cpp, benchmarks/bench_formatting.cpp</files>
  <action>
    1. Configure CMake with -DBUILD_BENCHMARKS=ON in Release mode
    2. Build the 3 benchmark executables
    3. Run each executable and capture the output (ns/op for all 18 benchmarks)
    4. Record the raw results for use in Task 2
  </action>
  <verify>All 3 executables run successfully and produce ns/op output for all 18 benchmarks</verify>
  <done>AC-1 satisfied: All benchmarks build and run without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create before/after comparison in BASELINE.md</name>
  <files>benchmarks/BASELINE.md</files>
  <action>
    1. Read the existing BASELINE.md (Phase 1 baseline data)
    2. Update BASELINE.md to include:
       - Original "v0.2.0 Baseline" section preserved
       - New "v0.3.0 Post-Optimization" section with current results
       - Comparison table with columns: Benchmark, Baseline ns/op, Current ns/op, Change %
       - Summary of key improvements and any remaining hot spots
    3. Note the optimization techniques that contributed to each improvement category
  </action>
  <verify>BASELINE.md contains both baseline and current data with percentage comparisons for all 18 benchmarks</verify>
  <done>AC-2 satisfied: Before/after comparison documented with all 18 benchmarks. AC-3 satisfied: Key targets analyzed with accurate change percentages</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- benchmarks/bench_harness.h (benchmark harness is stable)
- benchmarks/bench_lookup.cpp (benchmark code unchanged for fair comparison)
- benchmarks/bench_interpolation.cpp (benchmark code unchanged for fair comparison)
- benchmarks/bench_formatting.cpp (benchmark code unchanged for fair comparison)
- include/i18ncpp.h (no optimization changes in this phase)
- src/i18ncpp.cpp (no optimization changes in this phase)

## SCOPE LIMITS
- This plan measures and documents only — no code optimization changes
- Do not modify benchmark harness or benchmark test code
- Do not add new benchmarks (fair comparison requires same tests)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All 3 benchmark executables build in Release mode
- [ ] All 18 benchmarks produce valid ns/op results
- [ ] BASELINE.md has before/after comparison table
- [ ] Percentage changes calculated correctly
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- BASELINE.md contains comprehensive before/after performance comparison
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.paul/phases/05-profiling-validation/05-01-SUMMARY.md`
</output>
