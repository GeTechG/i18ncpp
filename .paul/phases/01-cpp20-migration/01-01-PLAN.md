---
phase: 01-cpp20-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [CMakeLists.txt, conanfile.py]
autonomous: true
---

<objective>
## Goal
Switch the project build configuration from C++17 to C++20 and verify all existing code compiles and passes tests.

## Purpose
C++20 unlocks heterogeneous lookup for unordered_map, std::format, std::span, constexpr improvements — all needed by subsequent v0.4 phases.

## Output
- CMakeLists.txt updated to C++20
- conanfile.py updated with C++20 compiler settings
- All 82 tests passing under C++20
- All 3 benchmark targets compiling
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Files
@CMakeLists.txt
@conanfile.py
@tests/CMakeLists.txt
@benchmarks/CMakeLists.txt
</context>

<acceptance_criteria>

## AC-1: C++20 Standard Set in Build
```gherkin
Given the project CMakeLists.txt
When CMAKE_CXX_STANDARD is read
Then it equals 20
```

## AC-2: All Existing Tests Pass
```gherkin
Given the project built with C++20
When ctest runs all test targets
Then all 82 tests pass with no failures
```

## AC-3: Benchmarks Compile
```gherkin
Given the project built with C++20 and BUILD_BENCHMARKS=ON
When benchmark targets are built
Then bench_lookup, bench_interpolation, bench_formatting all compile without errors
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update CMake C++ standard to C++20</name>
  <files>CMakeLists.txt</files>
  <action>
    Change `set(CMAKE_CXX_STANDARD 17)` to `set(CMAKE_CXX_STANDARD 20)` in root CMakeLists.txt line 4.
    No other CMakeLists.txt files need changes — they inherit from root.
  </action>
  <verify>grep "CMAKE_CXX_STANDARD 20" CMakeLists.txt</verify>
  <done>AC-1 satisfied: CMAKE_CXX_STANDARD is 20</done>
</task>

<task type="auto">
  <name>Task 2: Build and run all tests under C++20</name>
  <files>CMakeLists.txt</files>
  <action>
    Configure CMake with BUILD_TESTS=ON and BUILD_BENCHMARKS=ON.
    Build all targets.
    Run ctest to verify all 82 tests pass.
    If any C++20 deprecation warnings or errors appear, fix them in this task.
    
    Build commands:
    - cmake -B build -DBUILD_TESTS=ON -DBUILD_BENCHMARKS=ON
    - cmake --build build --config Release
    - cd build && ctest --output-on-failure -C Release
  </action>
  <verify>ctest output shows 82 tests passed, 0 failures</verify>
  <done>AC-2 and AC-3 satisfied: all tests pass, all benchmarks compile</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/i18ncpp.cpp (no source code changes in this phase)
- include/i18ncpp.h (no header changes in this phase)
- tests/*.cpp (no test code changes unless C++20 compat requires it)
- benchmarks/*.cpp (no benchmark code changes unless C++20 compat requires it)

## SCOPE LIMITS
- Only build configuration changes — no feature work
- Do not add new dependencies
- Do not change API signatures
- If source changes are needed for C++20 compat, keep them minimal and document

</boundaries>

<verification>
Before declaring plan complete:
- [ ] CMakeLists.txt has CMAKE_CXX_STANDARD 20
- [ ] cmake configure succeeds with no errors
- [ ] cmake --build succeeds with no errors
- [ ] ctest shows all 82 tests passing
- [ ] bench_lookup, bench_interpolation, bench_formatting compile
- [ ] No new warnings introduced
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Zero test regressions
- Build configuration is C++20
</success_criteria>

<output>
After completion, create `.paul/phases/01-cpp20-migration/01-01-SUMMARY.md`
</output>
